# =============================================================================
# SOMLENG COMPLETE INFRASTRUCTURE CONFIGURATION
# =============================================================================

# Infrastructure
RESTART_POLICY=unless-stopped
DOCKER_SUBNET=172.20.0.0/16

# Network Configuration
PUBLIC_IP=YOUR_PUBLIC_IP_HERE
DOMAIN=yourdomain.com

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=somleng

# Rails Configuration
RAILS_ENV=production
SECRET_KEY_BASE=your_secret_key_base_here

# FreeSWITCH Configuration
FS_EVENT_SOCKET_PASSWORD=your_fs_password_here
FS_LOG_LEVEL=info

# Background Jobs
SIDEKIQ_CONCURRENCY=10

# AWS Configuration (if using S3/other AWS services)
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# SMS Configuration
SMS_GATEWAY_MODE=http
SMPP_HOST=your_smpp_host
SMPP_PORT=2775
SMPP_USERNAME=your_smpp_username
SMPP_PASSWORD=your_smpp_password

# Somleng API Configuration
SOMLENG_API_URL=http://somleng_web:3000

# =============================================================================
# PRODUCTION SCALING CONFIGURATION
# =============================================================================

# Resource Limits
POSTGRES_SHARED_BUFFERS=512MB
POSTGRES_EFFECTIVE_CACHE_SIZE=2GB
POSTGRES_WORK_MEM=8MB

# Monitoring
PROMETHEUS_RETENTION_TIME=30d
GRAFANA_ADMIN_PASSWORD=your_grafana_password

# Security
JWT_SECRET=your_jwt_secret_here
API_KEY_SECRET=your_api_key_secret_here

# Telephony Provider Configuration
SIP_DOMAIN=sip.yourdomain.com
SIP_REALM=yourdomain.com

# Media Configuration
RTP_PORT_MIN=16384
RTP_PORT_MAX=65535

# Load Balancer Configuration
LB_ALGORITHM=round_robin
LB_HEALTH_CHECK_INTERVAL=30